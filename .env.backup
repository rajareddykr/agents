# Copy to .env. Everything here is OPTIONAL — the app runs fully offline on
# deterministic mock data with no configuration at all.

# --- Live market data (optional) -----------------------------------------
# Paste a Finnhub key to switch FIN MCP / NEWS MCP from mock to live data.
# FINNHUB_API_KEY=your_finnhub_key_here
# LIVE=1        # force live (needs a key) ; LIVE=0 forces mock

# --- Run mode / ports (optional) -----------------------------------------
# MODE=single           # single-process (default is distributed)
# HOST=127.0.0.1
# PORT=8000             # dashboard
# FIN_PORT=8071         # financial agent worker
# RES_PORT=8072         # research agent worker

# NOTE: there are intentionally NO AGT_* / mesh / bootstrap settings here.
# Those are added when you follow docs/AGT_MIGRATION_GUIDE.md.
LIVE=1
FINNHUB_API_KEY=d9gapf9r01qubrlg1fkgd9gapf9r01qubrlg1fl0



# ================= AGT control plane =================
AGT_CP_URL=http://localhost:20355
#AGT_CP_URL=https://api.sqa1.appsoc.com/aisec-agents-gov
AGT_ORG_CODE=demodevelop
AGT_GOVERNANCE_ENABLED=true
AGT_HEARTBEAT_SECONDS=10
AGT_LOG_LEVEL=DEBUG
AGT_FAIL_OPEN=true
AGT_AGENT_ID=did:mesh:7ffc48c841fc09a2dca56f5d
AGT_CP_TOKEN=agt_M8JnMSnCYZ_B1XSKnMfH_MlMaVtcCfprNu8c91vqCHc
# operator creds for auto-mint (defaults are demoadmin/changeme if omitted):
AGT_OPERATOR_USER=demoadmin
AGT_OPERATOR_PASSWORD=changeme
